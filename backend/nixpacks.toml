[setup]
pkgs = ["python311", "postgresql_16.dev", "gcc"]

[install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate --noinput && gunicorn sic_project.wsgi --bind 0.0.0.0:$PORT"
